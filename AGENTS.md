# Volunteer Hub — хакатон (дедлайн 20.07 09:00, UTC+5)

## Контекст (НЕ переспрашивать пользователя, работать автономно)

Хакатон «Startup Platform для партнёра», соло. Партнёр выбран: **Employee Volunteer Program (NVS & Zhurekten Zhurekke)** — программа корпоративного волонтёрства Chevron в Казахстане (субботники, посадка деревьев, поддержка детей с инвалидностью и из малообеспеченных семей; города: Астана, Алматы, Атырау).

**Продукт:** Volunteer Hub — веб-приложение, где координатор публикует волонтёрские события, а сотрудники записываются на них в 1 клик. Боль: сейчас записи на такие акции ведутся в рассылках/Excel, координатор не видит, кто и сколько записался.

**Критерии хакатона:** работающий продукт (40%), релевантность сфере партнёра (25%), фидбэк 5+ реальных пользователей (20%), питч (15%).

## Стек (ничего не добавлять!)

- Один статический сайт: `index.html` + `app.js` + `config.js`. Без фреймворков, без сборки, без npm-пакетов в рантайме.
- Supabase через CDN: `<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>` (глобальный `window.supabase`).
- БД: Supabase (схема в `schema.sql`, пользователь уже применил её и вставил ключи в `config.js`).
- Деплой: Vercel, статика, `npx vercel --prod --yes`.
- UI на русском. Стили — один `<style>` в index.html, минимально прилично.

## Функционал (всё, больше ничего)

1. Список событий из таблицы `events`: название, город, дата, категория, описание, сколько записалось (`signups`), кнопка «Записаться».
2. Запись на событие: имя + контакт → insert в `signups`. После записи обновить список.
3. «Я координатор»: кнопка открывает форму добавления события; поле «код организатора», сверка со строкой `zhurek2026` в app.js. Неверный код — alert, не сохранять. (ponytail: код косметический, спам-защита не нужна на хакатон)
4. Форма «Оставить отзыв о платформе»: имя/роль, оценка 1–5, текст → insert в `feedback`. Это сбор фидбэка для хакатона.

## Задачи — по порядку. После КАЖДОЙ: проверка → если падает, чинить самому → git commit.

1. `index.html` + `app.js` + `config.js`: подключение Supabase, вывод списка событий.
   Проверка: `node test.mjs` — скрипт ниже, писать GET events возвращает строки.
2. Запись на событие (insert в signups, обновление счётчика).
   Проверка: test.mjs POST signup → 201.
3. Форма координатора с кодом. Проверка: открыть страницу через `python3 -m http.server 8080` и curl — страница отдаётся; логику проверить глазами по коду + node-синтаксис `node --check app.js`.
4. Форма отзыва. Проверка: test.mjs POST feedback → 201.
5. `README.md`: партнёр, проблема, стек, запуск (открыть ссылку Vercel / локально `python3 -m http.server`).
6. `FEEDBACK.md`: шаблон таблицы на 5 пользователей (роль, когда тестировал, что сказал, подтверждение, что изменили) + раздел «Выводы». Заполняет пользователь, фальсифицировать нельзя.
7. Деплой: `npx vercel --prod --yes`. Если просит логин — написать пользователю точную команду `npx vercel login` и остановиться, всё остальное должно быть готово.
8. Финальный `node test.mjs` против прод-URL конфига + commit.

`test.mjs`: node-скрипт, читает URL/ключ из `config.js` (через fs+regex, без импорта), делает fetch в Supabase REST (`/rest/v1/events?select=*` с заголовками apikey/Authorization), assert что статус 200 и массив непустой; POST signup/feedback с `Prefer: return=minimal`, assert 201. Тестовые строки с пометкой "тест" — пользователь удалит в дашборде.

## Этап 2 — большое обновление (выполнять, если сказано «этап 2»)

Этап 1 уже сделан и задеплоен: https://volunteer-hub-kappa.vercel.app (список, запись, координатор, отзыв — всё работает, test.mjs зелёный). `index.html` УЖЕ обновлён под новые фичи (не переписывай его с нуля!). Твоя работа — переписать `app.js` и довести фичи до конца. Страница сейчас в рабочем состоянии даже со старым app.js — не сломай её.

После КАЖДОЙ фичи: `node --check app.js` + `node test.mjs` (все OK) + `python3 -m http.server 8080` & curl 200 + git commit. Падает — чини сам.

### Фичи (по порядку, по одному коммиту на фичу)

1. **Статистика** в `#stats`: три блока «N событий / M записалось волонтёров / K мест свободно». Данные из уже загружаемого `events + signups`.
2. **Остаток мест**: на карточке «Записалось: X · Осталось: Y из Z». Y = spots − signups.length. Y <= 0 → кнопка «Записаться» disabled, текст «Мест нет» (класс .full).
3. **Фильтр по городам** в `#filters`: кнопки «Все» + уникальные города из данных. Активная — класс .active. Клик фильтрует список (клиентский state, без новых запросов).
4. **Панель координатора**: после верного кода `zhurek2026` сохрани sessionStorage 'coord'='1', покажи кнопку `#dashBtn` (убрать .hidden). Панель `#dashboard`: по каждому событию — заголовок, счётчик, таблица участников (имя, контакт), кнопка «Скопировать список» → navigator.clipboard.writeText("Событие\\nИмя — контакт\\n..."), alert «Скопировано». Кнопка «Выйти» чистит sessionStorage и прячет панель.
5. **Поделиться**: на каждой карточке кнопка «🔗 Поделиться» (btn-share) → clipboard URL + `#ev-<id>`; карточке дай id `ev-<id>`. При загрузке, если location.hash = #ev-N → scrollIntoView + класс .hl.
6. **RU/KZ переключатель**: кнопка «ҚАЗ»/«РУС» в header. Словарь `T = {ru:{...}, kz:{...}}` для всех статических строк (заголовки, лейблы форм, кнопки) и динамических («Записаться»→«Тіркелу», «Записалось»→«Тіркелген», «Осталось»→«Қалды», «Мест нет»→«Орын жоқ», категории: Экология→Экология, Дети→Балалар, Образование→Білім, Здоровье→Денсаулық). Статике в index.html дай id и меняй textContent/placeholder в setLang(). Язык в localStorage 'lang', даты через toLocaleDateString(lang==='kz'?'kk-KZ':'ru-RU'). Проверка: глазами по коду, оба языка рендерятся.
7. **README.md**: дописать список фич (пп. 1–6) и прод-URL.
8. **Редеплой**: `npx vercel --prod --yes` (логин уже есть), потом `curl -s -o /dev/null -w "%{http_code}" <прод-URL>` = 200, `node test.mjs` зелёный, commit.

## Этап 3 — killer-фичи (выполнять, если сказано «этап 3»)

Этапы 1–2 сделаны и задеплоены: https://volunteer-hub-kappa.vercel.app — всё работает, test.mjs зелёный. Все фичи этапа 3 — ТОЛЬКО клиентские (app.js + index.html). **БД и schema.sql НЕ трогать** (нет доступа к SQL). Не ломай существующее: после каждой фичи `node --check app.js` + `node test.mjs` (зелёный) + `python3 -m http.server 8555` & curl 200 + git commit. Порядок = приоритет; если времени мало, делай сверху вниз.

1. **CSV-экспорт** (панель координатора, рядом с «Скопировать список»): кнопка «Скачать CSV» → CSV `Имя,Контакт\r\n...` с BOM `\uFEFF` (кириллица в Excel), Blob, `<a download="title.csv">`.click(). i18n: `csv_btn: 'Скачать CSV' / 'CSV жүктеу'`.
2. **«📅 В календарь»** на карточке события: кнопка → генерирует .ics (BEGIN:VCALENDAR/VEVENT, DTSTART;VALUE=DATE=YYYYMMDD из ev.date, SUMMARY=title, LOCATION=city, DESCRIPTION), Blob text/calendar, download `event-<id>.ics`. i18n: `cal_btn`.
3. **Сертификат волонтёра**: после успешной записи (и в «Моих записях») кнопка «🏅 Сертификат» → `<canvas>` 900x640: градиентный фон зелёный, рамка, 🌱, «Сертификат», имя крупно, «за участие в», название события, дата, город → `canvas.toDataURL()` → download `certificate.png`. Все строки через T (i18n): `cert_title: 'Сертификат'/'Сертификат'`, `cert_for: 'за участие в'/'қатысқаны үшін'`. Красиво — это для демо-видео.
4. **«Мои записи» (паспорт волонтёра)**: новая секция перед отзывом (добавь в index.html: `<section id="passport">` с input + кнопкой). Ввод контакта → фильтр `EVENTS` по `signups[].contact` (trim, lowercase) → список событий, где записан + «N событий · ~3N часов» (ponytail: 3ч фикс на событие, точный учёт — если будет поле в БД). Контакт в localStorage 'me', при загрузке подставлять. i18n: `passport_title: 'Мои записи'/'Менің тіркелуім'` и т.д.
5. **WhatsApp/Telegram-шаринг**: на карточке рядом с «Поделиться» две маленькие кнопки-ссылки: `https://wa.me/?text=<encodeURIComponent(title + ' ' + url)>` и `https://t.me/share/url?url=<url>&text=<title>`, target=_blank.
6. **Топ волонтёров**: секция внизу (перед footer): агрегация всех signups по name, топ-5, «Имя — N записей». Только если 1–5 готовы и test.mjs зелёный. i18n: `top_title`.
7. **README.md**: дописать фичи 1–6. **Редеплой** `npx vercel --prod --yes`, curl прод = 200, `node test.mjs` зелёный, commit.

## Этап 4 — харденинг и багфиксы (выполнять ПОСЛЕ этапа 3)

Цель: продукт без дыр. После каждого пункта: `node --check app.js` + `node test.mjs` + commit. Если на этап 3 времени ушло много — пропусти этап 3 пп. 4–6, но этап 4 пп. 1–5 ОБЯЗАТЕЛЬНЫ (это живые баги).

1. **Антидубль записи**: в doSignup перед insert проверь `ev.signups` (уже в EVENTS): если contact совпал (trim, lowercase) — покажи msg, НЕ вставляй. i18n `already_signed: 'Вы уже записаны на это событие'/'Сіз бұл іс-шараға тіркелгенсіз'`.
2. **Отмена записи**: в «Моих записях» у каждого события кнопка «Отменить» → `supabase.from('signups').delete().match({event_id, contact})` → loadEvents. Ошибка = alert «Отмена недоступна», не падать. i18n `cancel_btn: 'Отменить'/'Болдырмау'`. (RLS-политику delete пользователь добавил; если нет — просто alert.)
3. **Анти-double-click**: во всех async-обработчиках (doSignup, addEvent, sendFeedback) кнопку `disabled=true` до await, включить обратно в finally.
4. **Прошедшие события**: date < сегодня → карточке класс `.past` (opacity .6), вместо кнопки «Записаться» текст t('past'): 'Событие прошло'/'Іс-шара өтті'. Сортировка: будущие по дате ↑, потом прошедшие ↓.
5. **Фильтр-XSS**: сейчас город вставляется в inline onclick строкой — сломается на кавычке. Переделай: кнопкам `onclick="setCity(INDEX)"`, setCity(i) берёт из массива городов по индексу. Заодно esc() на fallback категории.
6. **Сеть**: try/catch вокруг всех supabase-вызовов → catch показывает t('net_err'): 'Нет соединения. Проверьте интернет.'/'Байланыс жоқ. Интернетті тексеріңіз.'
7. **a11y/полировка**: в setLang — `document.documentElement.lang = lang==='kz'?'kk':'ru'`; CSS: `.card{animation:fadeIn .3s}` + `@keyframes fadeIn{from{opacity:0;transform:translateY(6px)}}`; `.btn:focus-visible{outline:2px solid #74c69d;outline-offset:2px}`; `.past{opacity:.6}`.
8. **test.mjs усилить**: (а) статическая проверка — все `getElementById('X')` из app.js существуют как `id="X"` в index.html, кроме динамических с префиксами `name- contact- signup- signupMsg- ev-`; (б) DELETE-проверка signups (если 204 — политика delete есть, залогировать OK; если ошибка — просто warning, не падать). Тест должен оставаться зелёным.
9. **Финал**: README дописать (фичи этапа 3–4), редеплой `npx vercel --prod --yes`, curl прод = 200, `node test.mjs` зелёный, commit, `git log --oneline` показать.

## Этапы 5–7 — от прототипа к продукту, который покупают

Этапы 1–4 задеплоены и работают. Цель: выглядеть как SaaS, который Chevron может взять себе, а другая компания — купить. Приоритет фич: (боль реальна × видно в демо × не ломает существующее). Всё клиентское (app.js/index.html/config.js/test.mjs). После каждой фичи: `node --check app.js` + `node test.mjs` (зелёный) + serve/curl 200 + git commit. Редеплой после каждого этапа. Прод не ломать.

### Боли по ролям (зачем эти фичи)

- **Волонтёр:** подтверждение записи тонет в почте — «а записан ли я вообще?»; на годовом performance review нечем подтвердить волонтёрский вклад; записываться в пустой список страшновато («а вдруг никто не пойдёт?»); экспатам Chevron нужен английский.
- **Координатор:** квартальный отчёт в HQ = вечер с Excel (события × записи × часы вручную); еженедельный субботник пересоздаётся с нуля каждый раз; опечатку в опубликованном событии не исправить; напомнить участникам о завтрашнем событии — ручной обход.
- **HR/руководство:** ESG/CSR-метрики (часы, охват, города) собираются вручную; при защите бюджета программы нечего показать на одном экране.
- **НКО-партнёр (Zhurekten Zhurekke):** нужен печатный список участников на входе и прозрачность, кто реально придёт.
- **Покупатель со стороны:** пустой экран в первый день выглядит как «сломано»; ребрендинг = переписывание кода; новичок не понимает, что делать в первые 10 секунд; продукт только на русском = не продаётся международно.

### Этап 5 — вовлечение волонтёра + impact

1. **Третий язык (EN)**: кнопка `#langToggle` циклит ru→kz→en→ru, показывает код следующего языка (ҚАЗ/ENG/РУС). `T.en` — полный дубль всех ключей (качественный перевод; категории: Ecology, Children, Education, Health). Даты: en-GB. `documentElement.lang='en'`. В `test.mjs` добавь проверку паритета: у T.ru, T.kz, T.en одинаковый набор ключей (regex-извлечение ключей из блоков).
2. **Impact-полоса** (между about и filters): 4 цифры из EVENTS — «Часов волонтёрства: signups×3 · Волонтёров: уникальных contact · Событий: всего · Городов: уникальных». Заголовок i18n `impact_title`: 'Наш вклад'/'Біздің үлесіміз'/'Our impact'. Это ESG-нарратив для демо.
3. **Социальное доказательство на карточке**: если signups.length>0 → строка с первыми 3 именами + «+N» (`more`: 'ещё'/'тағы'/'more'). Лечит «страх пустого списка».
4. **Уровень волонтёра в паспорте**: 1–2 события = 🌱 `level_1` (Новичок/Жаңадан/Beginner), 3–4 = 🌿 `level_2` (Активист/Белсенді/Activist), 5+ = 🌳 `level_3` (Лидер/Көшбасшы/Leader). Прогресс-бар до следующего уровня (`next_level`: 'До следующего уровня: N событий' и т.п.).
5. **Авто-паспорт**: если localStorage 'me' заполнен — при загрузке сразу выполни lookupPassport (подставь contact и вызви без клика).
6. **«Как это работает»**: полоса из 3 шагов с эмодзи под about: «1️⃣ Выберите событие · 2️⃣ Запишитесь в 1 клик · 3️⃣ Получите сертификат». i18n `step1/2/3`. Компактно, одна строка.

### Этап 6 — координатор и HR

1. **Отчёт координатора** (в панели, сверху): итоги — событий / записей / ~часов / средняя заполняемость %. У каждого события fill-bar: процент = signed/spots (цвет: <50% красный, <80% жёлтый, иначе зелёный). Кнопка «Скопировать отчёт» → markdown (заголовок с датой, таблица событий с цифрами, итоги) в clipboard + alert. i18n `report_title`, `copy_report`, `fill_rate`.
2. **Дублировать событие**: кнопка «⧉» в панели → `prompt(t('dup_prompt'), date+7дней)` → insert копии события с новой датой → loadEvents. i18n `dup_btn` ('Дублировать'...), `dup_prompt`.
3. **Редактировать событие**: кнопка «Ред.» в панели → форма добавления открывается заполненной, режим edit (editingId), submit → update().eq('id', editingId). Ошибка (нет update-политики) → alert и скрыть кнопки ред./удалить навсегда (флаг canEdit=false). i18n `edit_btn`, `save_btn`, `edit_unavail`.
4. **Удалить событие**: кнопка «🗑» → confirm → delete().eq('id') → loadEvents; ошибка → alert + canEdit=false. i18n `confirm_del`.
5. **Скопировать все контакты**: кнопка в панели → все contact через «, » в clipboard (для BCC-рассылки) + alert. i18n `copy_contacts`.
6. **Печать списка**: `@media print` — показывать только #dashboard (таблицы), скрыть header/filters/кнопки; кнопка «🖨 Печать» в панели → window.print(). i18n `print_btn`.

### Этап 7 — продукт для покупателя

1. **Whitelabel-конфиг**: в config.js `window.COMPANY = {name:'Chevron', program:'Employee Volunteer Program — NVS & Zhurekten Zhurekke', emoji:'🌱', color:'#2d6a4f'}`. Используй везде: h1 = emoji + ' Volunteer Hub', about_title из COMPANY.program, favicon — COMPANY.emoji, цвет темы через CSS-переменную `--brand` (замени хардкод #2d6a4f на var(--brand) в стилях; `document.documentElement.style.setProperty('--brand', COMPANY.color)`). Проверка: поменять config → весь сайт перекрашивается.
2. **Empty states**: нет событий → 🌱 крупно + `empty_events` («Пока нет событий — загляните позже» и т.п.); пустой паспорт → `empty_passport`; пустой топ → `empty_top`; ничего не найдено поиском → `empty_search`. Центрировано, серо, дружелюбно.
3. **Поиск**: input `#search` над списком (placeholder `search_ph`), клиентский фильтр по title+description+city (lowercase), комбинируется с фильтром городов.
4. **PWA**: `icon.svg` (эмодзи 🌱 в svg), `manifest.json` (name, short_name, start_url '.', display standalone, theme_color #2d6a4f, icons → icon.svg sizes any), `<link rel="manifest">` + `<meta name="theme-color">` в head. Без service worker.
5. **Мобильная полировка**: `@media (max-width:480px)`: stats gap меньше, все кнопки ≥44px высотой (touch), share-row переносится, шрифты не мельче 12px.

### SQL для пользователя (НЕ выполнять моделью, выдать отдельным блоком в финальном ответе)

```sql
create policy "edit event" on events for update to anon using (true) with check (true);
create policy "delete event" on events for delete to anon using (true);
```

Без этого редактирование/удаление покажет alert и скроет кнопки — graceful-деградация, падать не должно.

### Wow-моменты для демо-видео (для пользователя, не для кода)

1. Ссылка из WhatsApp → запись в 1 клик → сертификат PNG прямо на телефоне.
2. Переключение RU → ҚАЗ → ENG на глазах (международный продукт).
3. Паспорт волонтёра: контакт → мои события, часы, уровень 🌳 → скачать сертификат.
4. Координатор: fill-бары → «Скопировать отчёт» → вставка в чат: «квартальный отчёт за 10 секунд вместо вечера в Excel».
5. Impact-полоса: часы / волонтёры / города — ESG-нарратив для Chevron.
6. Дублирование еженедельного субботника одной кнопкой.
7. Whitelabel: 4 строки в config.js — и это уже продукт другой компании.
8. Печать списка участников для НКО на входе.

## Правила

- Минимум кода. Никаких новых зависимостей, абстракций, папок, роутеров.
- Не задавать пользователю вопросов до конца списка. Ошибки чинить самому.
- Не ходить в интернет, кроме деплоя.
- Коммиты: `feat: список событий`, `feat: запись на событие` и т.п., после каждой зелёной проверки.
- Если config.js содержит заглушки "ВСТАВЬ" — сразу написать пользователю одну строку: «вставь ключи из Supabase в config.js» — и продолжить всё, что можно без БД.
